- title(@document.title)
= page_title(@document.title)
= link_to("Back", documents_url)
%table{:width => "100%", :border => "0", :cellpadding => "0", :cellspacing => "2"}
  %thead
    %tr.row-header
      %th
        Field name
      %th
        Value
  - (@document.attributes.keys-['_id', 'title']).each do |field|
    %tr
      %td{width: '30%'}
        = field
      %td
        = @document.do_or_do_not(field)
